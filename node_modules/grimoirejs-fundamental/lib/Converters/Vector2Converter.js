import { Vector2 } from "grimoirejs-math";
function Vector2Converter(val) {
    if (val instanceof Vector2) {
        return val;
    }
    else {
        return Vector2.parse(val);
    }
}
export default Vector2Converter;
//# sourceMappingURL=Vector2Converter.js.map