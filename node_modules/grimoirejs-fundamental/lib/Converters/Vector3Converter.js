import { Vector3 } from "grimoirejs-math";
function Vector3Converter(val) {
    if (val instanceof Vector3) {
        return val;
    }
    else {
        return Vector3.parse(val);
    }
}
export default Vector3Converter;
//# sourceMappingURL=Vector3Converter.js.map