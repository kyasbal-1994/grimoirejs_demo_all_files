import ResourceBase from "./ResourceBase";
export default class Texture2D extends ResourceBase {
    readonly texture: WebGLTexture;
    magFilter: number;
    minFilter: number;
    wrapS: number;
    wrapT: number;
    private _texParameterChanged;
    private _magFilter;
    private _minFilter;
    private _wrapS;
    private _wrapT;
    constructor(gl: WebGLRenderingContext);
    update(level: number, width: number, height: number, border: number, format: number, type: number, pxiels?: ArrayBufferView, flipY?: boolean): void;
    update(image: HTMLImageElement, flipY?: boolean): void;
    register(registerNumber: number): void;
    destroy(): void;
    private _checkSize(img);
    private _updateTexParameter();
}
