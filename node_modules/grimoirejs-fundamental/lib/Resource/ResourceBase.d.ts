import IDObject from "grimoirejs/lib/Base/IDObject";
declare abstract class ResourceBase extends IDObject {
    gl: WebGLRenderingContext;
    destroyed: boolean;
    validPromise: Promise<ResourceBase>;
    private _valid;
    private _validResolve;
    valid: boolean;
    constructor(gl: WebGLRenderingContext);
    destroy(): void;
}
export default ResourceBase;
