import IRenderRendererMessage from "../Messages/IRenderRendererMessage";
import IBufferUpdatedMessage from "../Messages/IBufferUpdatedMessage";
import Component from "grimoirejs/lib/Node/Component";
import IAttributeDeclaration from "grimoirejs/lib/Node/IAttributeDeclaration";
export default class RenderQuadComponent extends Component {
    static attributes: {
        [key: string]: IAttributeDeclaration;
    };
    private _targetBuffer;
    private _gl;
    private _canvas;
    private _fbo;
    private _geom;
    private _clearColor;
    private _clearColorEnabled;
    private _clearDepth;
    private _clearDepthEnabled;
    private _materialContainer;
    $awake(): void;
    $mount(): void;
    $bufferUpdated(args: IBufferUpdatedMessage): void;
    $render(args: IRenderRendererMessage): void;
}
