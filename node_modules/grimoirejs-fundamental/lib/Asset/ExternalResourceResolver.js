import CacheResolver from "./CacheResolver";
export default class ExternalResourceResolver extends CacheResolver {
    constructor() {
        super(ExternalResourceResolver._toAbsolute);
    }
    static _toAbsolute(href) {
        const link = document.createElement("a");
        link.href = href;
        return (link.protocol + "//" + link.host + link.pathname + link.search + link.hash);
    }
}
//# sourceMappingURL=ExternalResourceResolver.js.map