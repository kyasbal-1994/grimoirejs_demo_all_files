import Matrix from "./Matrix";
declare class MatrixArray {
    rawElements: Float32Array;
    static getIdentityMatrixArray(length: number): MatrixArray;
    constructor(length: number);
    getAt(index: number): Matrix;
    setAt(index: number, matrix: Matrix): void;
}
export default MatrixArray;
