import Vector3 from "./Vector3";
import Matrix from "./Matrix";
import AABB from "./AABB";
declare class PointList {
    points: Vector3[];
    static initializeWithCube(list: PointList): PointList;
    constructor(pointList?: PointList);
    addPoint(point: Vector3): void;
    transform(transformMatrix: Matrix): void;
    clear(): void;
    getBoundingBox(): AABB;
    toMathematicaPoints(): string;
}
export default PointList;
